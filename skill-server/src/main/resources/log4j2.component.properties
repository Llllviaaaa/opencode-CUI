# Async logging is configured via <AsyncRoot> in log4j2-spring.xml (mixed async mode).
# This preserves ThreadContext (MDC) propagation to async thread.
# Note: Do NOT use AsyncLoggerContextSelector (global async) as it breaks ThreadContext snapshot.
