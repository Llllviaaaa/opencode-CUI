<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.opencode.cui.skill.repository.SysConfigMapper">

    <resultMap id="SysConfigResultMap" type="com.opencode.cui.skill.model.SysConfig">
        <id     property="id"          column="id"/>
        <result property="configType"  column="config_type"/>
        <result property="configKey"   column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="description" column="description"/>
        <result property="status"      column="status"/>
        <result property="sortOrder"   column="sort_order"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <sql id="selectAllColumns">
        SELECT id, config_type, config_key, config_value,
               description, status, sort_order,
               created_at, updated_at
        FROM sys_config
    </sql>

    <select id="findByTypeAndKey" resultMap="SysConfigResultMap">
        <include refid="selectAllColumns"/>
        WHERE config_type = #{configType}
          AND config_key  = #{configKey}
        LIMIT 1
    </select>

    <select id="findByType" resultMap="SysConfigResultMap">
        <include refid="selectAllColumns"/>
        WHERE config_type = #{configType}
        ORDER BY sort_order ASC
    </select>

    <select id="findById" resultMap="SysConfigResultMap">
        <include refid="selectAllColumns"/>
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config
            (config_type, config_key, config_value,
             description, status, sort_order,
             created_at, updated_at)
        VALUES
            (#{configType}, #{configKey}, #{configValue},
             #{description}, #{status}, #{sortOrder},
             NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE sys_config
        SET config_type  = #{configType},
            config_key   = #{configKey},
            config_value = #{configValue},
            description  = #{description},
            status       = #{status},
            sort_order   = #{sortOrder},
            updated_at   = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_config
        WHERE id = #{id}
    </delete>

</mapper>
