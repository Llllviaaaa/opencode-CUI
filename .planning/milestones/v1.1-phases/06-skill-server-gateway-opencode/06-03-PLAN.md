---
phase: 6
plan: 3
title: Legacy session-path cleanup and multi-instance regression coverage
wave: 2
depends_on:
  - 1
  - 2
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - VER-01
  - VER-02
files_modified:
  - skill-server/src/main/java/com/opencode/cui/skill/service/GatewayRelayService.java
  - skill-server/src/main/java/com/opencode/cui/skill/service/RedisMessageBroker.java
  - skill-server/src/main/java/com/opencode/cui/skill/ws/SkillStreamHandler.java
  - documents/gateway-skill-routing/
  - .planning/REQUIREMENTS.md
autonomous: true
user_setup: []
---

<objective>
清理旧的 `session:{sessionId}` 实时订阅职责，并补上针对 A/B/C 多实例错投场景的回归验证与文档，让新链路成为唯一实时消费路径。
</objective>

<context>
@.planning/phases/06-skill-server-gateway-opencode/06-CONTEXT.md
@.planning/phases/06-skill-server-gateway-opencode/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 清理 session 广播在实时链路中的职责</name>
    <files>
      skill-server/src/main/java/com/opencode/cui/skill/service/GatewayRelayService.java
      skill-server/src/main/java/com/opencode/cui/skill/service/RedisMessageBroker.java
      skill-server/src/main/java/com/opencode/cui/skill/ws/SkillStreamHandler.java
    </files>
    <behavior>
      - Test 1: 实时回流链路不再调用 `ensureSessionSubscribed`、`subscribeToSessionBroadcast`、`unsubscribeFromSession` 这一类 session 订阅入口
      - Test 2: 如果某些 session 相关逻辑仅用于历史恢复，会与实时 user-stream 路径明确隔离
    </behavior>
    <action>梳理并下线 `session:{sessionId}` 在实时消费路径中的职责，删除或迁移 `ensureSessionSubscribed`、`subscribeToSessionBroadcast`、`unsubscribeFromSession` 等逻辑；如果仍有 session 级逻辑服务于历史恢复、snapshot 或 reconnect 语义，要把这类职责从实时链路中隔离出来并加注释，避免双活广播。</action>
    <verify>
      <automated>mvn -pl skill-server -DskipTests compile</automated>
    </verify>
    <done>实时推送路径只剩 `user-stream:{userId}`，旧的 session 订阅逻辑不会再参与实时消费竞争。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 补充多实例错投场景的自动化回归验证</name>
    <files>
      skill-server/src/test
      ai-gateway/src/test
    </files>
    <behavior>
      - Test 1: 用户连接在 A、REST 发消息在 B、gateway 回流落在 C 时，A 能收到消息，B 不会错误消费
      - Test 2: 同一用户如果在两个实例上都持有有效流连接，两边都能收到同一条 user-stream 消息
    </behavior>
    <action>为 gateway 与 skill-server 增加最小但关键的自动化测试覆盖，重点验证 `userId` 校验、回流补全、user-stream 广播，以及 A/B/C 错投链路下的正确消费结果；如果现有测试基建不足，先补测试 scaffold，再写回归用例。</action>
    <verify>
      <automated>mvn -pl ai-gateway,skill-server test -Dtest=*Gateway*Test,*Relay*Test,*Stream*Test</automated>
    </verify>
    <done>至少有一组自动化测试能证明 A/B/C 错投链路已修复，且多实例多连接行为符合预期。</done>
  </task>

  <task type="auto">
    <name>Task 3: 更新设计与规划文档，固化新链路约定</name>
    <files>
      documents/gateway-skill-routing/
      .planning/REQUIREMENTS.md
      .planning/ROADMAP.md
    </files>
    <action>更新设计文档和规划文档，明确新的 Redis channel 命名、`GatewayMessage.userId` 语义、gateway 的 `ak -> userId` 校验职责，以及实时链路不再使用 `session:{sessionId}` 的约束；把验证入口和联调关注点写清楚，避免执行阶段完成后文档仍停留在旧模型。</action>
    <verify>
      <automated>mvn -pl ai-gateway,skill-server -DskipTests compile</automated>
    </verify>
    <done>文档、requirements 与实现术语一致，后续执行和联调都以 user-stream 模型为唯一实时链路。</done>
  </task>
</tasks>

<must_haves>
- 旧的 session 实时广播路径必须退出实时消费链路，不能与 user-stream 双活。
- 自动化验证要覆盖“连接在 A、发送在 B、回流在 C”的核心复现链路。
- 文档、配置和代码术语必须全部切到 user-stream / userId 模型，避免实现和认知再次分叉。
</must_haves>

<success_criteria>
- `session:{sessionId}` 不再承担实时广播与消费职责
- 存在可重复运行的自动化回归验证来证明多实例错投问题被修复
- 设计文档与规划文档都已更新为新的 user-stream 模型
</success_criteria>
