# Requirements Archive: v1.1 Connection-Aligned Consumption Fix

**Archived:** 2026-03-12

## Delivered Requirements

- [x] `BUG-01`: 同一 `sessionId` 的连续流式事件只由真实持有客户端连接的 `skill-server` 实例消费
- [x] `BUG-02`: REST 命中非连接实例不会改变后续 AI 返回消息的消费归属
- [x] `BUG-03`: A/B/C 错投场景下，用户仍能稳定看到完整回复
- [x] `VER-01`: 文档、Redis channel/key 命名与实现保持一致
- [x] `VER-02`: 具备覆盖“连接在 A、发送在 B、回流在 C”场景的验证方案，并已完成真实 UAT

## Outcomes

- 实时广播通道切换为 `user-stream:{userId}`
- `gateway` 维护并使用 `ak -> userId` 做链路校验和回流补全
- 旧 `session:{sessionId}` 实时广播路径退出主链路
- 真实环境已验证 miniapp 可以实时收到 opencode 返回

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| BUG-01 | Phase 6 | Complete |
| BUG-02 | Phase 6 | Complete |
| BUG-03 | Phase 6 | Complete |
| VER-01 | Phase 6 | Complete |
| VER-02 | Phase 6 | Complete |
