---
phase: 8
plan: 1
title: Unified Snowflake rules and generator scaffolding
wave: 1
depends_on: []
requirements:
  - SESS-01
files_modified:
  - skill-server/src/main/java/
  - ai-gateway/src/main/java/
  - skill-server/src/main/resources/
  - ai-gateway/src/main/resources/
autonomous: true
user_setup: []
---

<objective>
在 `skill-server` 与 `gateway` 中分别落同一套 Snowflake ID 生成实现，并统一 epoch、service bits、service code、worker bits、sequence bits、时钟回拨策略与配置语义，为后续全量主键雪花化提供统一基础设施。
</objective>

<context>
@.planning/phases/08-gateway-global-session-identity/08-CONTEXT.md
@.planning/phases/08-gateway-global-session-identity/08-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 在两个服务中引入同规则 Snowflake 生成器</name>
    <files>
      skill-server/src/main/java/
      ai-gateway/src/main/java/
    </files>
    <behavior>
      - Test 1: `skill-server` 与 `gateway` 的生成器对同一组配置遵循同一 bit layout
      - Test 2: 两边生成的 id 均为 `Long`，可直接作为当前 Java 主键类型使用
      - Test 3: 不同 `serviceCode` 下生成的 id 不会发生冲突
    </behavior>
    <action>分别在 `skill-server` 与 `gateway` 中新增镜像 Snowflake 生成器实现与配置类，统一 epoch、service bits、worker bits、sequence bits 与时钟回拨策略，避免两边后续规则漂移。</action>
    <verify>
      <automated>mvn -pl skill-server,ai-gateway -DskipTests compile</automated>
    </verify>
    <done>两个服务都具备同规则的 Snowflake 主键生成能力，且配置语义保持一致。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 明确 service code 与 worker 配置约束</name>
    <files>
      skill-server/src/main/resources/
      ai-gateway/src/main/resources/
      documents/
    </files>
    <behavior>
      - Test 1: `skill-server` 与 `gateway` 的默认配置使用不同 `serviceCode`
      - Test 2: 配置缺失或非法时会在启动或初始化阶段被明确拒绝
    </behavior>
    <action>为两个服务增加统一的 Snowflake 配置项，并明确默认 `serviceCode`、`workerId`、epoch 与合法范围；同时补充说明文档，固定这套治理约束。</action>
    <verify>
      <automated>mvn -pl skill-server,ai-gateway -DskipTests compile</automated>
    </verify>
    <done>Snowflake 配置规则与 service/worker 分配边界已固定，后续主键改造可直接复用。</done>
  </task>
</tasks>

<must_haves>
- 两边 Snowflake 生成器规则完全一致
- `serviceCode` 必须进入算法结构而不是只存在文档中
- 生成器输出类型保持 `Long`
</must_haves>

<success_criteria>
- `skill-server` 与 `gateway` 都可生成统一规则的 Snowflake ID
- 默认配置下两边 service code 不冲突
- 后续各实体主键雪花化不需要再发明新的发号规则
</success_criteria>
