---
phase: 8
plan: 3
title: gateway primary key snowflake migration
wave: 3
depends_on:
  - 1
requirements:
  - SESS-01
files_modified:
  - ai-gateway/src/main/resources/db/migration/
  - ai-gateway/src/main/resources/mapper/
  - ai-gateway/src/main/java/com/opencode/cui/gateway/service/
  - ai-gateway/src/main/java/com/opencode/cui/gateway/model/
  - ai-gateway/src/test/java/
autonomous: true
user_setup: []
---

<objective>
将 `gateway` 中纳入本 phase 的核心持久化实体切换到 Snowflake 主键，重点覆盖 `agent_connection`、`ak_sk_credential` 以及其创建、复用、查询链路，确保 gateway 与 skill-server 共用同一套 ID 治理规则。
</objective>

<context>
@.planning/phases/08-gateway-global-session-identity/08-CONTEXT.md
@.planning/phases/08-gateway-global-session-identity/08-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: gateway DDL 与 mapper 去除自增主键依赖</name>
    <files>
      ai-gateway/src/main/resources/db/migration/
      ai-gateway/src/main/resources/mapper/AgentConnectionMapper.xml
      ai-gateway/src/main/resources/mapper/AkSkCredentialMapper.xml
    </files>
    <behavior>
      - Test 1: `agent_connection.id` 不再依赖 `AUTO_INCREMENT`
      - Test 2: `ak_sk_credential.id` 不再依赖 `AUTO_INCREMENT`
      - Test 3: mapper insert 改为显式写入主键
    </behavior>
    <action>新增 gateway 侧 migration 与 mapper 调整，移除关键持久化表对数据库自增与 `useGeneratedKeys` 的依赖，为应用侧 Snowflake 主键落地铺平路径。</action>
    <verify>
      <automated>mvn -pl ai-gateway -DskipTests compile</automated>
    </verify>
    <done>`gateway` 的核心持久化表已具备应用侧 Snowflake 主键写入能力。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: gateway 创建与复用链路切换到 Snowflake 主键</name>
    <files>
      ai-gateway/src/main/java/com/opencode/cui/gateway/service/AgentRegistryService.java
      ai-gateway/src/main/java/com/opencode/cui/gateway/service/
      ai-gateway/src/main/java/com/opencode/cui/gateway/model/
      ai-gateway/src/test/java/
    </files>
    <behavior>
      - Test 1: 新建 `agent_connection` 时由应用侧生成 Snowflake 主键
      - Test 2: 复用现有 `agent_connection` 记录的逻辑不回归
      - Test 3: `findById`、`findLatestByAkId` 等依赖主键或排序的路径仍正确
    </behavior>
    <action>将 gateway 中依赖自增主键的创建链路切换到应用侧生成 Snowflake 主键，并补充覆盖“新建 + 复用 + 查询”的测试，防止只修 insert 场景。</action>
    <verify>
      <automated>mvn -pl ai-gateway test -Dtest=*Agent*Test,*Auth*Test</automated>
    </verify>
    <done>`gateway` 的核心持久化实体已纳入统一 Snowflake ID 规则，且复用型业务逻辑保持正确。</done>
  </task>
</tasks>

<must_haves>
- `gateway` 与 `skill-server` 使用同一套 Snowflake 规则
- 不只验证新建路径，还要验证复用与查询路径
- 关键 mapper 不再依赖数据库回填主键
</must_haves>

<success_criteria>
- `gateway` 核心实体主键已雪花化
- 注册、查询、状态更新和复用逻辑不回归
- 两边主键基础设施治理规则保持一致
</success_criteria>
