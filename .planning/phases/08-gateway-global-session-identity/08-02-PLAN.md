---
phase: 8
plan: 2
title: skill-server primary key snowflake migration
wave: 2
depends_on:
  - 1
requirements:
  - SESS-01
  - SESS-02
files_modified:
  - skill-server/src/main/resources/db/migration/
  - skill-server/src/main/resources/mapper/
  - skill-server/src/main/java/com/opencode/cui/skill/service/
  - skill-server/src/main/java/com/opencode/cui/skill/controller/
  - skill-server/src/test/java/
autonomous: true
user_setup: []
---

<objective>
将 `skill-server` 中几乎所有当前依赖数据库自增主键的核心实体统一切换到 Snowflake ID，重点覆盖 `skill_session`、`skill_message`、`skill_message_part` 及其直接关联链路，确保 `welinkSessionId` 继续作为会话主键但不再依赖自增。
</objective>

<context>
@.planning/phases/08-gateway-global-session-identity/08-CONTEXT.md
@.planning/phases/08-gateway-global-session-identity/08-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: skill-server DDL 与 mapper 去除自增主键依赖</name>
    <files>
      skill-server/src/main/resources/db/migration/
      skill-server/src/main/resources/mapper/SkillSessionMapper.xml
      skill-server/src/main/resources/mapper/SkillMessageMapper.xml
      skill-server/src/main/resources/mapper/SkillMessagePartMapper.xml
    </files>
    <behavior>
      - Test 1: `skill_session.id` 不再依赖 `AUTO_INCREMENT`
      - Test 2: `skill_message.id` 与 `skill_message_part.id` 不再使用 `useGeneratedKeys`
      - Test 3: 直接关联的 `session_id` / `message_id` 外键引用在新 schema 下保持一致
    </behavior>
    <action>新增 migration 调整 `skill-server` 相关表主键策略，移除 `AUTO_INCREMENT`，同步修改 MyBatis mapper，使 insert 直接写入预生成的 Snowflake 主键。</action>
    <verify>
      <automated>mvn -pl skill-server -DskipTests compile</automated>
    </verify>
    <done>`skill-server` 的核心持久化表不再依赖数据库自增主键与回填机制。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: skill-server 创建与持久化链路改为应用侧生成主键</name>
    <files>
      skill-server/src/main/java/com/opencode/cui/skill/service/SkillSessionService.java
      skill-server/src/main/java/com/opencode/cui/skill/service/SkillMessageService.java
      skill-server/src/main/java/com/opencode/cui/skill/service/MessagePersistenceService.java
      skill-server/src/main/java/com/opencode/cui/skill/service/GatewayRelayService.java
      skill-server/src/main/java/com/opencode/cui/skill/controller/
    </files>
    <behavior>
      - Test 1: `createSession` 返回的 `welinkSessionId` 为 Snowflake ID
      - Test 2: 消息与消息分片的主键由应用侧生成，不依赖 DB 回填
      - Test 3: `welinkSessionId <-> toolSessionId` 既有链路不回归
    </behavior>
    <action>将 `skill-server` 的 session/message/message part 创建路径统一切换为应用层预生成 Snowflake 主键，保留现有 `Long` 类型与协议字段语义，确保对外链路仍以 `welinkSessionId` 作为会话主标识。</action>
    <verify>
      <automated>mvn -pl skill-server test -Dtest=*Session*Test,*Message*Test,*GatewayRelayServiceTest,*SkillStreamHandlerTest</automated>
    </verify>
    <done>`skill-server` 已完成从数据库自增到 Snowflake 主键的主链路迁移，协议行为保持兼容。</done>
  </task>
</tasks>

<must_haves>
- `welinkSessionId` 继续是会话主键，但生成方式改为 Snowflake
- `skill_message` 与 `skill_message_part` 必须同批改造，不能留下主从主键策略混用
- 不改变当前 Java 侧 `Long` 类型接口
</must_haves>

<success_criteria>
- `skill-server` 的核心主键体系已雪花化
- 现有创建、消息持久化、回流映射和流式广播链路不回归
- mapper 中不再依赖 `useGeneratedKeys`
</success_criteria>
