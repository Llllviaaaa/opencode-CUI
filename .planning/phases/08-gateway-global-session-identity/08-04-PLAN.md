---
phase: 8
plan: 4
title: test environment rebuild and end-to-end snowflake verification
wave: 4
depends_on:
  - 2
  - 3
requirements:
  - SESS-02
  - SAFE-02
files_modified:
  - skill-server/src/test/java/
  - ai-gateway/src/test/java/
  - documents/
  - .planning/REQUIREMENTS.md
autonomous: true
user_setup: []
---

<objective>
基于“测试环境允许清数重建”的前提，完成新 Snowflake 主键体系的测试环境重建、双服务回归验证和文档同步，确保 `skill-server + gateway` 在统一 ID 基础设施下稳定运行。
</objective>

<context>
@.planning/phases/08-gateway-global-session-identity/08-CONTEXT.md
@.planning/phases/08-gateway-global-session-identity/08-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 清理测试环境存量数据并按新规则重建</name>
    <files>
      skill-server/src/main/resources/db/migration/
      ai-gateway/src/main/resources/db/migration/
      documents/
    </files>
    <behavior>
      - Test 1: 测试环境可在无历史兼容负担下完成 schema 重建
      - Test 2: 重建后新插入数据全部使用 Snowflake 主键
    </behavior>
    <action>基于当前仍处于测试环境的前提，明确清理旧数据、重建 schema 和重新初始化测试数据的操作方式，并把这套流程记录到文档或执行说明中。</action>
    <verify>
      <automated>mvn -pl skill-server,ai-gateway -DskipTests compile</automated>
    </verify>
    <done>测试环境已具备按新主键策略重建并重新建数的明确路径。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 补齐统一 Snowflake 主键的跨服务回归验证</name>
    <files>
      skill-server/src/test/java/
      ai-gateway/src/test/java/
      .planning/REQUIREMENTS.md
    </files>
    <behavior>
      - Test 1: `skill-server` 创建会话时返回 Snowflake `welinkSessionId`
      - Test 2: `gateway` 注册和持久化新实体时使用 Snowflake 主键
      - Test 3: 创建、更新、回流、流式消息、权限回复等链路在新主键规则下不回归
      - Test 4: service code 配置错误或冲突时有明确失败或告警路径
    </behavior>
    <action>围绕统一 Snowflake 主键体系补齐自动化回归测试与需求同步，确认 `skill-server + gateway` 在主键雪花化后仍满足业务链路与可观测性要求。</action>
    <verify>
      <automated>mvn -pl skill-server test</automated>
      <automated>mvn -pl ai-gateway test</automated>
    </verify>
    <done>主键雪花化后的双服务链路已完成自动化验证，需求与设计说明同步到最新状态。</done>
  </task>
</tasks>

<must_haves>
- 测试环境采用清数重建，不引入长期双轨兼容
- 自动化验证必须覆盖 `skill-server` 与 `gateway`
- 文档与需求必须同步反映统一 Snowflake 规则
</must_haves>

<success_criteria>
- 测试环境可按新主键规则稳定重建
- `skill-server + gateway` 双服务在统一 Snowflake 主键下回归通过
- Phase 8 交付后不再依赖数据库自增主键
</success_criteria>
