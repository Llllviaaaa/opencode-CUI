---
phase: 7
plan: 3
title: Structured routing observability and regression coverage
wave: 3
depends_on:
  - 1
  - 2
requirements:
  - SAFE-01
  - ROUT-02
files_modified:
  - ai-gateway/src/main/java/com/opencode/cui/gateway/service/
  - ai-gateway/src/main/java/com/opencode/cui/gateway/ws/
  - ai-gateway/src/test/java/com/opencode/cui/gateway/service/
  - ai-gateway/src/test/java/com/opencode/cui/gateway/ws/
  - documents/
  - .planning/REQUIREMENTS.md
autonomous: true
user_setup: []
---

<objective>
为多 `source` 路由补齐结构化观测与回归验证，让 `gateway` 在出现 `source` 校验失败、owner 丢失、同域 fallback 等场景时，既能严格拒绝错投，又能通过 `traceId` 和结构化日志快速定位问题。
</objective>

<context>
@.planning/phases/07-gateway-multi-service-source-isolation/07-CONTEXT.md
@.planning/phases/07-gateway-multi-service-source-isolation/07-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 为多 source 路由引入 traceId 与结构化日志字段</name>
    <files>
      ai-gateway/src/main/java/com/opencode/cui/gateway/service/
      ai-gateway/src/main/java/com/opencode/cui/gateway/ws/
    </files>
    <behavior>
      - Test 1: 关键路由日志会记录 `traceId`、`source`、`instanceId`、`ownerKey`、`messageType`、`routeDecision`、`fallbackUsed`、`errorCode`
      - Test 2: `source_not_allowed`、`source_mismatch`、owner 路由失败等异常都有明确日志语义
    </behavior>
    <action>在 `gateway` 的关键握手、路由、fallback、错误返回链路引入统一 `traceId`，并补齐结构化日志字段。把“同域重选成功”和“同域重选失败”从普通 debug 文本提升为明确的路由事件，确保 SAFE-01 的可观测性要求落地。</action>
    <verify>
      <automated>mvn -pl ai-gateway -DskipTests compile</automated>
    </verify>
    <done>多 `source` 路由关键节点具备统一 `traceId` 与结构化日志，问题定位不再依赖零散文本搜索。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 补齐多 source 错投与 fallback 的回归验证</name>
    <files>
      ai-gateway/src/test/java/com/opencode/cui/gateway/service/
      ai-gateway/src/test/java/com/opencode/cui/gateway/ws/
    </files>
    <behavior>
      - Test 1: 新服务消息绝不会回给 `skill-server`
      - Test 2: `source` 校验失败时会协议级拒绝且不进入主链路
      - Test 3: 原 owner 不可用时可同域 fallback，但同域无目标时会失败而不是跨域发送
    </behavior>
    <action>围绕 `source` 校验、owner 域隔离、同域 fallback 和错投保护补齐自动化回归测试。重点覆盖“新服务请求不会回到 `skill-server`”这个核心场景，以及 `source` 不一致、owner 缺失、fallback 失败等边界情形。</action>
    <verify>
      <automated>mvn -pl ai-gateway test -Dtest=*RelayServiceTest,*EventRelayServiceTest,*SkillWebSocketHandlerTest</automated>
    </verify>
    <done>至少有一组自动化测试可以稳定证明多 `source` 路由不会跨服务错投。</done>
  </task>

  <task type="auto">
    <name>Task 3: 更新需求与设计说明，固化新的路由契约</name>
    <files>
      .planning/REQUIREMENTS.md
      documents/
    </files>
    <action>将多 `source` 标准协议、`source + owner` 路由规则、同域 fallback 限制和结构化观测约束同步到需求或设计说明中，避免执行阶段完成后只有代码变化、没有统一文档语义。特别要写清楚 `source` 不下沉到 `pc-agent`，以及 `gateway` 只提供标准协议、不做服务私有适配。</action>
    <verify>
      <automated>mvn -pl ai-gateway,skill-server -DskipTests compile</automated>
    </verify>
    <done>代码、需求和设计说明对多 `source` 路由契约的表述保持一致。</done>
  </task>
</tasks>

<must_haves>
- 关键路由决策必须具备统一 `traceId` 与结构化字段。
- “新服务消息不会回给 `skill-server`”必须有回归验证覆盖。
- 错投保护与同域 fallback 的规则必须在日志和文档里都能被看见。
</must_haves>

<success_criteria>
- `gateway` 关键路由链路具备统一 `traceId` 和结构化日志
- 存在覆盖跨服务错投与同域 fallback 的自动化回归验证
- 文档与需求已同步到新的多 `source` 路由契约
</success_criteria>
