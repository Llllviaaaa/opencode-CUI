---
phase: 10
plan: 3
title: lock stream affinity regressions with focused backend coverage
wave: 3
depends_on:
  - 1
  - 2
requirements:
  - STREAM-01
  - STREAM-02
  - STREAM-03
files_modified:
  - skill-server/src/test/java/com/opencode/cui/skill/service/GatewayRelayServiceTest.java
  - skill-server/src/test/java/com/opencode/cui/skill/ws/SkillStreamHandlerTest.java
  - skill-server/src/test/java/com/opencode/cui/skill/service/
autonomous: true
user_setup: []
---

<objective>
用后端自动化测试把 Phase 10 的会话亲和性基线锁死：既证明实时事件总带正确 `welinkSessionId`，也证明恢复失败时不会误广播，为 Phase 11 的前端并发分流验证打稳定基础。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-stream-event-affinity/10-01-PLAN.md
@.planning/phases/10-stream-event-affinity/10-02-PLAN.md
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: 增补广播负载断言，覆盖实时增量与恢复态</name>
    <files>
      skill-server/src/test/java/com/opencode/cui/skill/service/GatewayRelayServiceTest.java
      skill-server/src/test/java/com/opencode/cui/skill/ws/SkillStreamHandlerTest.java
    </files>
    <behavior>
      - Test 1: `tool_event`、`tool_done`、`tool_error`、`permission_request` 广播负载都显式断言 `welinkSessionId`
      - Test 2: `snapshot` 与 `streaming` 恢复态测试继续断言 `welinkSessionId`，确保与实时增量基线一致
      - Test 3: 至少有一组测试断言消息内部原始 `sessionId` 与最终对外 `welinkSessionId` 的纠偏关系
    </behavior>
    <action>把现有偏“contains 某类型字符串”的测试升级为对 JSON 结构和 `welinkSessionId` 数值的显式断言，防止未来字段遗漏时仍然假通过。</action>
    <verify>
      <automated>mvn -pl skill-server test -Dtest=GatewayRelayServiceTest,SkillStreamHandlerTest</automated>
    </verify>
    <done>后端测试能直接证明实时增量事件和恢复态事件都满足统一的会话标识契约。</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: 增补跨会话安全回归场景</name>
    <files>
      skill-server/src/test/java/com/opencode/cui/skill/service/GatewayRelayServiceTest.java
      skill-server/src/test/java/com/opencode/cui/skill/service/
    </files>
    <behavior>
      - Test 1: 构造 A/B 两个 session，仅 A 的 `toolSessionId` 能命中时，广播只会落到 A 的用户流
      - Test 2: 构造恢复失败场景时，验证不存在错误的 Redis 广播或持久化副作用
      - Test 3: 关键测试命名与断言能直接映射 `STREAM-01` / `STREAM-02` / `STREAM-03`
    </behavior>
    <action>新增针对跨会话串流风险的回归测试，用最小但明确的 A/B 双会话夹具证明 Phase 10 修复的是“用户可见串流”根因，而不只是字段格式问题。</action>
    <verify>
      <automated>mvn -pl skill-server test -Dtest=GatewayRelayServiceTest</automated>
    </verify>
    <done>Phase 10 的关键风险被自动化测试锁住，后续进入 Phase 11 时只需要验证前端分流行为，不再担心后端出口继续漏字段或错投。</done>
  </task>
</tasks>

<must_haves>
- 测试必须直接断言 JSON 中的 `welinkSessionId`，不能只验证消息类型或广播次数
- 至少有一组测试覆盖 A/B 双会话隔离，证明修复目标是用户可见的串流风险
- 恢复失败路径必须同时验证“无广播”和“无额外错误副作用”
</must_haves>

<success_criteria>
- 后端测试对实时增量与恢复态都建立了显式 `welinkSessionId` 断言
- A/B 双会话安全回归场景有自动化覆盖
- Phase 10 的三个 requirement 都能在测试命名和断言中找到直接证据
</success_criteria>
