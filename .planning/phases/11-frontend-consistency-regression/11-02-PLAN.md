---
phase: 11
plan: 2
title: tighten multi-session filtering and switching regression coverage
wave: 2
depends_on:
  - 1
requirements:
  - CONSIST-02
  - SAFE-03
files_modified:
  - skill-miniapp/src/hooks/useSkillStream.ts
  - skill-miniapp/src/pages/
  - skill-miniapp/
autonomous: true
user_setup: []
---

<objective>
把 miniapp 的会话过滤和切换场景验证收紧成明确的前端回归基线，确保 A/B session 并发、切会话和恢复态续接这三类场景不会再退化。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-frontend-consistency-regression/11-01-PLAN.md
</context>

<tasks>
  <task type="auto" tdd="false">
    <name>Task 1: 复核前端会话过滤边界</name>
    <files>
      skill-miniapp/src/hooks/useSkillStream.ts
      skill-miniapp/src/pages/
    </files>
    <behavior>
      - `snapshot`、`streaming`、实时增量消息对当前页面都遵循同一 `welinkSessionId` 过滤规则
      - 不带当前会话标识的异常事件不会被错误渲染到页面
      - 会话切换时，上一会话的进行中状态不会残留到下一会话视图
    </behavior>
    <action>审视 `handleStreamMessage` 与页面消费链路，确认前端分流规则只以当前 `sessionId` 为准，不依赖隐式的旧状态残留。</action>
    <verify>
      <automated>npm run typecheck</automated>
    </verify>
    <done>A/B session 并发显示隔离的前端边界被重新确认，不再混入恢复态例外。</done>
  </task>

  <task type="auto" tdd="false">
    <name>Task 2: 沉淀可重复执行的回归检查清单</name>
    <files>
      .planning/phases/11-frontend-consistency-regression/
    </files>
    <behavior>
      - 明确列出 A/B session 并发、切回会话续接、重连续接三类检查点
      - 每个检查点都能直接映射 `CONSIST-01/02/03` 与 `SAFE-03`
      - 结果可以被后续 `verify-work` 或 milestone audit 直接引用
    </behavior>
    <action>把本 phase 的前端回归重点整理成 verification artifact，避免仅靠聊天记录保留验收上下文。</action>
    <verify>
      <manual>检查 verification artifact 是否显式覆盖 A/B session 隔离、恢复态分流和切会话/重连续接三类场景。</manual>
    </verify>
    <done>Phase 11 的前端回归目标有了可复核的书面清单和 requirement 映射。</done>
  </task>
</tasks>

<must_haves>
- A/B session 并发场景必须单独列成检查项
- 切会话和重连后的进行中回复续接必须被视为 requirement 级目标，而不是体验优化
- verification artifact 必须能被后续 UAT 直接复用
</must_haves>

<success_criteria>
- miniapp 的会话过滤逻辑和恢复逻辑被统一到同一套前端规则
- Phase 11 的回归关注点可以脱离聊天上下文独立理解
- 后续 verify-work 可以直接基于该计划继续做人工验收
</success_criteria>
